interface:
  display_name: "月度培训数据处理"
  short_description: "整理月度培训数据、复核异常并生成年度看板与汇报材料"
  default_prompt: "Use $monthly-training-data to process this month's training files and prepare the review outputs."

policy:
  allow_implicit_invocation: true


